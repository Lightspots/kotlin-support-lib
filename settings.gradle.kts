pluginManagement {
    repositories {
        gradlePluginPortal()
        jcenter()
    }
}

rootProject.name = "kotlin-support-lib"

include("kotlin-support-lib-core")
include("kotlin-support-testing")
